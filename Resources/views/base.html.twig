<!DOCTYPE html>
<html>
    <head {{ sonata_seo_html_attributes() }}>
        {{ sonata_seo_title() }}
        {{ sonata_seo_metadatas() }}

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('bundles/eluceoevent/css/bootstrap.css') }}">
            <link rel="stylesheet" href="{{ asset('bundles/eluceoevent/css/event.css') }}">
        {% endblock %}
    </head>

    <body>

        <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="brand" href="{{ path('_welcome') }}">EluceoEventBundle</a>
                    <ul class="nav">
                        <li>
                            <a href="{{ path('_welcome') }}">{{ 'Home'|trans({}, 'EluceoEventBundle') }}</a>
                        </li>
                        <li>
                            <a href="{{ path('eluceo.event.index') }}">{{ 'Events'|trans({}, 'EluceoEventBundle') }}</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="container">
            <div class="row">
                <div class="span3">
                    Display categories here...
                </div>

                <div class="span9">
                    {% block main %}
                    {% endblock %}
                </div>
            </div>
        </div>

    </body>
</html>